// Re-export from refactored meetings module for backwards compatibility
// The actual implementation is now in ./meetings/Meetings.tsx
import Meetings from './meetings/Meetings';
export default Meetings;
