import { DEFAULT_BASE_URL } from '../config';

export const ENDPOINTS = {
    UPDATE_STARTUP_PROFILE: `${DEFAULT_BASE_URL}/api/startup/details`,
    CREATE_NOTIFICATION: `${DEFAULT_BASE_URL}/api/notifications`,
};
